import React from 'react';

const ValidationComponent = (props) => {

    const validationComponent = <div >{props.validation}</div>;

    return validationComponent;
}


export default ValidationComponent;